{
  "name": "api-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for the users API (Jest + Axios)",
  "scripts": {
    "test": "jest --runInBand"
  },
  "dependencies": {
    "axios": "^1",
    "jest": "^29"
  },
  "jest": {
    "testTimeout": 30000
  }
}
