# PostgreSQL configuration
# NOTE: These are test credentials for the exercise. In a real project the .env
# file must NEVER be committed to git - it goes into .gitignore, and a
# .env.example (without real values) is committed instead.
DB_HOST=db
DB_USER=postgres
DB_PASS=admin
DB_NAME=db
